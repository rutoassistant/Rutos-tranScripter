# 0001: ArithmeticEnv as the sole Environment implementation

The Ruto's transScripter transpiler supports pluggable Environments — each maps natural-language words to Python code generators. Only `arithmeticEnv.py` exists. We keep it as the single Environment until a second language domain is needed.